import {
  connection0,
  connection1,
  connection2,
  connection3,
  connection4,
  connection5,
  connection6,
  connection7,
  connection8,
  connection9,
} from "../config/db";
export const conecctions = [
  connection0.promise(),
  connection1.promise(),
  connection2.promise(),
  connection3.promise(),
  connection4.promise(),
  connection5.promise(),
  connection6.promise(),
  connection7.promise(),
  connection8.promise(),
  connection9.promise(),
];
